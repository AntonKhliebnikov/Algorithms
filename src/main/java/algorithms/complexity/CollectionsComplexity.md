# 📘 Big O — сложность операций для коллекций Java

## Что такое Big O
**Big O (О-большое)** — это способ показать, **как быстро (или медленно)** работает алгоритм при росте количества данных `n`.

Big O не измеряет секунды — она описывает **зависимость времени выполнения** от размера входных данных.

| Обозначение | Название | Пояснение простыми словами |
|--------------|-----------|-----------------------------|
| **O(1)**     | Константная | Время не зависит от количества элементов. Пример — доступ по индексу в массиве. |
| **O(log n)** | Логарифмическая | Каждый шаг уменьшает задачу вдвое (пример — бинарный поиск). |
| **O(n)**     | Линейная | Чем больше элементов, тем дольше. Один проход по массиву. |
| **O(n log n)** | Квазилинейная | Типичная сложность сортировок (Merge Sort, Quick Sort). |
| **O(n²)**    | Квадратичная | Два вложенных цикла — сравниваем каждый с каждым. |
| **O(2ⁿ)**    | Экспоненциальная | Очень медленно. Полный перебор всех вариантов. |

---

## 💡 Сравнение коллекций Java по операциям

| Операция | **ArrayList** | **LinkedList** | **HashMap / HashSet** | **TreeMap / TreeSet** |
|-----------|----------------|----------------|------------------------|------------------------|
| **1️⃣ Вставка по позиции** | **O(n)** — нужно сдвинуть элементы | **O(n)** — нужно дойти до нужного узла | — | **O(log n)** |
| **2️⃣ Вставка без позиции (в конец)** | **O(1)** — быстро (иногда O(n) при расширении массива) | **O(1)** — вставка в хвост | **O(1)** — по ключу (среднее время) | **O(log n)** |
| **3️⃣ Получение по индексу** | **O(1)** — прямой доступ | **O(n)** — идём по узлам | **O(1)** — по ключу | **O(log n)** |
| **4️⃣ Удаление по индексу** | **O(n)** — сдвиг элементов | **O(n)** — дойти до нужного узла | **O(1)** — по ключу | **O(log n)** |
| **5️⃣ Удаление по значению (не по индексу)** | **O(n)** — поиск элемента | **O(n)** — поиск по узлам | **O(1)** — по ключу (среднее) | **O(log n)** |
| **6️⃣ Contains (проверка наличия)** | **O(n)** — линейный поиск | **O(n)** — линейный поиск | **O(1)** — по хешу (среднее) | **O(log n)** — поиск по дереву |

---

## 🔍 Примечания
- **O(1)** — самая быстрая операция. Время не растёт с количеством элементов.
- **O(n)** — время растёт линейно. Чем больше данных, тем дольше.
- **O(log n)** — рост медленный. Даже при миллионе элементов добавляется всего ~20 шагов.
- У **HashMap/HashSet** сложности указаны для среднего случая. В худшем (при множестве коллизий) возможна **O(n)**.
- У **ArrayList** добавление в конец почти всегда O(1), но иногда O(n) при автоматическом увеличении размера массива.

---

## 📈 Краткое сравнение (в общем виде)

| Коллекция | Сильная сторона | Слабая сторона |
|------------|----------------|----------------|
| **ArrayList** | Быстрый доступ по индексу (`O(1)`) | Медленные вставки/удаления внутри списка (`O(n)`) |
| **LinkedList** | Быстрые вставки и удаления в начале/конце | Медленный доступ по индексу (`O(n)`) |
| **HashMap / HashSet** | Очень быстрые вставка, удаление, поиск (`O(1)`) | Нет порядка элементов, зависит от хеш-функции |
| **TreeMap / TreeSet** | Отсортированные элементы, поиск по диапазону (`O(log n)`) | Медленнее, чем хеш-таблица |

---

## 📚 Пример для понимания

Если в `ArrayList` 1 000 000 элементов:

- `add(el)` — добавить в конец → **O(1)**
- `add(0, el)` — вставить в начало → **O(n)** (нужно сдвинуть миллион элементов)
- `contains(el)` — поиск элемента → **O(n)**
- `get(500000)` — получить по индексу → **O(1)**

---

> 💬 **Итого:**  
> Big O помогает понять, насколько хорошо коллекция справится с большим объёмом данных.  
> Если нужно часто **искать** или **добавлять** элементы по ключу — выбирай `HashMap` / `HashSet`.  
> Если нужен **сортированный** порядок — `TreeMap` / `TreeSet`.  
> Если важен **порядок вставки** и быстрый доступ по индексу — `ArrayList`.
